# Day 3 · Tue Aug 4

**Built:** Chunker. Splits on numbered paragraphs when the numbering is actually ascending, falls back to blank lines then sentences. Every chunk carries the case number and parties so keyword search can find the doc from any chunk. 10 tests passing. Also applied to six income platforms.

**Learned:** Fixed the obvious cause of chunks going over the size limit and it only dropped from 11.9% to 4.81%. There was a second cause underneath. Would have shipped it if I hadn't asserted the invariant exactly.

**Stuck:** Shortest document dropped to 3,446 chars after HTML stripping. Some doc is mostly markup and I haven't looked at it.

`AI-103: none today` · `DSA 0` · `SQL 0`
