# Day 5 · Thu Aug 6

**Built:** Retrieval. Search is one matrix multiply plus argpartition for top-k, since cosine is just a dot product when vectors are normalized at index time. Guards for model and dimension mismatch, because those give you correctly shaped meaningless scores instead of an error. Index now stores chunk text so results can actually be shown.

**Learned:** Finished the Develop AI agents on Azure learning path. Also shortlisted three OSS issues to work on, one each in Ragas, dbt-core and DuckDB.

**Stuck:** First query came back with ranks 1 and 2 as consecutive chunks from the same judgment, so the top 3 only covered two documents. One document crowding out the rest means a better answer somewhere else never makes the list.

`AI-103: agents path done` · `DSA 0` · `SQL 0`
