# Day 4 · Wed Aug 5

**Built:** Embedding index. Normalizes vectors at write time so retrieval is one dot product instead of a dot product plus two norms. Writes in shards so an hour-long run survives an interrupt. Counts chunks that exceed the model limit. 16 tests, split so the fast ones run in 5s and the model-backed ones stay opt-in.

**Learned:** Andrej Karpathy video for NN from scratch, backpropagation intuition. micrograd video upto 1:15hrs reached.

**Stuck:** Still no Azure subscription for AI-103 hands-on. Cards are used up. Going to lean on practice assessments and docs since most of the exam is theory, but the Foundry SDK code questions will be a gap.

`AI-103: 0` · `Karpathy: micrograd to 1:15` · `DSA 4` · `SQL 0`
